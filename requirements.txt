# Pinned to versions actually tested against this codebase (test suite,
# benchmark suites, MCP server, and the production-hardening pass) --
# not just "whatever >= resolves to today". Bump deliberately, re-test
# before deploying a bump.
flask==3.1.3
flasgger==0.9.7.1
flask-cors==6.0.5
flask-limiter==4.1.1
gunicorn==26.0.0
numpy==2.4.4
pandas==3.0.2
scikit-learn==1.8.0
joblib==1.5.3
pyyaml==6.0.3
mcp==1.28.0
tree-sitter==0.25.2
tree-sitter-cpp==0.23.4
tree-sitter-java==0.23.5
